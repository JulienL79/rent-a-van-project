export type TAdminVehicleData = {
  id: string;
  brand: string;
  model: string;
  registrationPlate: string;
  cityName: string;
  userId: string;
  createdAt: Date;
};

export type TAdminVehicleRow = {
  id: string;
  registrationPlate: string;
  createdAt: Date;
};
