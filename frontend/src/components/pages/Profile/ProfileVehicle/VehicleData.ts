export const fuelTypeOptions = [
  "diesel",
  "petrol",
  "electric",
  "hybrid",
  "other",
].map((opt) => ({
  label: opt,
  value: opt,
}));

export const gearTypeOptions = ["manual", "automatic"].map((opt) => ({
  label: opt,
  value: opt,
}));
