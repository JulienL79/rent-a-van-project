export const capitalizeFirstChar = (string: string) => {
  return string.charAt(0).toUpperCase() + string.slice(1);
};

export const minimalizeString = (string: string) => {
  return string.toLowerCase();
};
